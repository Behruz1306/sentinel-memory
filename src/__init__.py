"""Sentinel Memory — secure, predictive, trust-aware retrieval middleware.

Sits between LiveKit (voice) and the knowledge base (Moss paradigm), deciding —
based on session trust, not just semantic similarity — what an AI agent may
retrieve, redact, block, or act on.
"""

__version__ = "0.2.0"
