"""Sentinel — the Retrieval Firewall for AI agents.

Every retrieval request an AI agent makes passes through Sentinel, which
decides — based on *trust*, not just semantic similarity — what may be
retrieved, redacted, or blocked.
"""

__version__ = "0.1.0"
