# Lean dependency set for the deployed dashboard + API server (server.py).
# The Firewall Console, Immune System, Red Team, and all /api routes work with
# just these. The in-browser Live Voice tab also mints LiveKit tokens via the
# lightweight livekit-api below. The full voice *worker* (livekit-agents, in the
# root requirements.txt) runs as a separate process/container, not in this image.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
python-multipart>=0.0.9   # PDF upload + Twilio form webhooks

# Moss — semantic retrieval + the threat-memory immune system.
moss>=1.4

# LLM (OpenAI / MiniMax / Qwen — all OpenAI-compatible via base_url).
openai>=1.0

# AWS CloudWatch breach logging (falls back to local file if no creds).
boto3>=1.34

# Mint LiveKit join tokens for the Live Voice tab (lightweight, no agents).
livekit-api>=1.1

# Workspace: PDF ingest (Unsiloed) + PDF session reports.
requests>=2.31
fpdf2>=2.7
