# Sentinel Memory — secure, predictive, trust-aware retrieval middleware
# NOTE: Moss SDK requires Python >=3.10. Recommended setup (uv-managed):
#   uv venv --python 3.12 .venv
#   uv pip install --python .venv -r requirements.txt
#
# Core
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Moss — real-time semantic retrieval (the retrieval backend Sentinel guards).
# Falls back to a local lexical index if absent.
moss>=1.4

# --- Optional integrations (everything degrades gracefully if absent) -------

# LLM for social-engineering reasoning + embeddings.
# Works for OpenAI AND MiniMax (MiniMax exposes an OpenAI-compatible endpoint;
# set SENTINEL_LLM_PROVIDER=minimax + MINIMAX_API_KEY). Also fits TrueFoundry /
# Bedrock gateways via SENTINEL_LLM_BASE_URL.
openai>=1.0

# AWS CloudWatch breach logging (falls back to local security_events.jsonl).
boto3>=1.34

# Voice transport — LiveKit Agents 1.5.x. STT/LLM/TTS run via LiveKit
# Inference (no OpenAI/Deepgram keys). The demo also runs fully offline via
# demo_call.py, so this is optional.
livekit-agents[silero,turn-detector]==1.5.16
